{
  "name": "soco-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "contentful": "^7.14.6",
    "nuxt": "^2.14.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
